name: MY CI

on:
  push:
    branches:
      - main


jobs:
  hello:
    name: My first job
    runs-on: ubuntu-latest
    step:
      - name: My first step
        run: echo "Hello world"
      - name: My second step
        run: mkdir -z test 